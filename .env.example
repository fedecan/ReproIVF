MSSQL_SA_PASSWORD=ChangeThis_StrongPass123!
JWT_KEY=CHANGE_THIS_FOR_A_LONG_RANDOM_SECRET_MIN_32_CHARS
# Only needed if browser calls WebAPI directly from another origin.
# If browser goes through client container (/api proxy), you can leave empty.
CORS_ALLOWED_ORIGINS=
